yarn add bulma
yarn add moment
객체를 배열로... 즉 부모의 객체를 받아서 map을 이용해서 배열로 만듦
UI컴포넌트 즉 함수형클래스에서는 props만 받고 state는 지정하지 않는다
yarn add react-router-dom
라우터에서 component는 소문자
a href 대신 Link 태그를 쓰는 이유는 리플레이시 방지
Link 태그 대신 NavLink를 쓰는 이유는 active 추가됨